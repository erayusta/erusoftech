# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.idea
.vscode
*.swp

# bootstrap artifact (regenerated by sandbox, not source of truth)
erusoftech.bundle

# raw video sources used to assemble hero-bg.mp4 (keep locally, don't track)
herobackground*.mp4

# raw brand source mockup (6.6 MB PNG); optimized WebP derivatives are the
# ones that actually ship — regenerate with scripts/build-brand-assets.sh
logo.png

# hero-bg master is a built artifact — rebuild with scripts/build-hero-bg.sh
# (prior commit introduced it to get the first working render; not re-tracking)
public/placeholders/hero-bg.mp4
